[pytest]
# Async test config for the orchestrator/router/verification subsystems.
asyncio_mode = auto
testpaths = tests
markers =
    live: hits the real Anthropic API (needs ANTHROPIC_API_KEY / backend/.env); excluded by `-m "not live"`
