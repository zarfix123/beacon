"""Tests for app/router/router.py (BUILD_INDEX.md §2 / step 19).

TODO: against a fake responder, assert all agent-activated emitted before any
response-item, one response-item per returned item, dispatch returns the flat list,
and the event dicts byte-match the frozen api-websocket.md payloads.

Placeholder — no real assertions yet.
"""
