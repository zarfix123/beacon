"""Tests for app/grant_access/ (BUILD_INDEX.md §2 / step 30).

TODO: assert toggle_visibility flips restricted->public on the right row only, replay
re-runs on the SAME query_id, the chunk's response-item is now full/verified, and the
flow is idempotent on a double-click. Assert /query wrote RunContext (else 404, OQ-8).

Placeholder — no real assertions yet.
"""
