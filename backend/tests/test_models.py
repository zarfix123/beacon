"""Tests for app/models.py (BUILD_INDEX.md §2 / step 2).

TODO: assert GatedResult.to_wire() emits exactly the 7 frozen response-item keys
(answer, source_party, source_doc_title, decision, verified, chunk_id,
source_agent_id) and nothing else — no `embedding`, no `text`, no `access_requestable`.

Placeholder — no real assertions yet.
"""
