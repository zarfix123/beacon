"""Tests for app/claude/redaction.py (BUILD_INDEX.md §2 / step 13).

TODO: against the seeded restricted servo chunk, assert the gist is non-empty and
one line, shares no >=6-word verbatim run with the source, is deterministic across
calls, and falls back to a content-free template on refusal/error.

Placeholder — no real assertions yet.
"""
