"""Tests for app/provenance/ (BUILD_INDEX.md §2 / step 16).

TODO: assert assemble_provenance never reads chunk text/embedding; internal fields
(timestamp, reason) never appear on the wire ResponseItem; build_response_item verifies
ONLY full items.

Placeholder — no real assertions yet.
"""
