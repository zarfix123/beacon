"""Tests for app/retrieval/search.py keyword stub (BUILD_INDEX.md §2 / step 8).

TODO: against the locked demo query, assert own-agent chunks only, all tiers present,
descending score, <= top_k, KeyError on a bogus agent_id, and [] on a no-keyword query.

Placeholder — no real assertions yet.
"""
