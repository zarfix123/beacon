"""Tests for gate leakage — the cardinal-sin test (BUILD_INDEX.md §2 / step 11).

TODO: assert no raw text/embedding appears in a non-full GatedResult; the restricted
chunk's distinctive solution token is absent from any redacted/denied `answer`.

Placeholder — no real assertions yet.
"""
