"""Tests for agent isolation — the security claim (BUILD_INDEX.md §2 / step 8).

TODO: assert searching agent A returns zero chunks owned by B or C; each AgentIndex
holds a separate chunk list (no shared references); set_visibility touches only one row.

Placeholder — no real assertions yet.
"""
