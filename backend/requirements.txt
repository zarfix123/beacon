# Relay backend — consolidated pip dependencies (BUILD_INDEX.md §5)
# MVP scope: no DB, no auth, no Chroma (plain numpy over tiny corpora).

fastapi
uvicorn[standard]          # ASGI server + websockets extra
pydantic>=2
# pydantic-settings        # REMOVED: incompatible with pinned pydantic 2.5.0; app/config.py
#                            uses a stdlib dataclass + manual .env loader instead.
anthropic>=0.40            # redaction / verification / synthesis Claude calls
# python-dotenv            # not installed here; app/config.py + app/claude/client.py load .env manually
numpy                      # cosine math + embedding matrices
model2vec                  # static embeddings (potion-retrieval-32M) — numpy-only inference, NO torch
rank-bm25                  # BM25 lexical channel for hybrid retrieval (RRF fusion)
pytest                     # unit tests
pytest-asyncio             # async orchestrator/router/verify tests
# chromadb                 # OPTIONAL — not used; plain numpy is simpler for tiny corpora
