"""Agents subsystem: 3 isolated party agents + their flat indexes.

See backend/docs/agents-corpus-index.md. This package owns the Agent abstraction,
genuine party isolation, per-agent indexes, and the embedding helpers. Retrieval
itself lives in app/retrieval/search.py.
"""
